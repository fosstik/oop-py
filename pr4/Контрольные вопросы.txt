1. Чем отличаются метод __new__ от __init__?
метод __new__ создаёт экземпляр класса, а метод __init__ его инициализирует. 
2. Для чего нужен класс super()
для вызова определённых методов и свойств в родительском классе.
